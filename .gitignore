# Python
__pycache__/
*.pyc

# Node deps
mcp/shared-wiki/node_modules/
node_modules/

# mcp/shared-wiki/dist/ is deliberately NOT ignored: the plugin cache has no install-time
# build step, so the bundled MCP must ship prebuilt for the plugin's .mcp.json to start it.
# Rebuild with `npm run build` in mcp/shared-wiki; release-check gates dist-vs-src drift.

# Machine-local install/config artifacts
grill-adapter.config.json
shared-wiki-mcp.config.json
manifest-output.json

# OS
.DS_Store
