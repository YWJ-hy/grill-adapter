# Python
__pycache__/
*.pyc

# Node deps
mcp/shared-wiki/node_modules/
node_modules/

# MCP dist/ directories are deliberately NOT ignored: the plugin cache has no install-time
# build step, so every bundled MCP must ship its prebuilt dist/index.js. Rebuild with npm run
# build in the affected mcp/<name>/ directory; release-check gates dist-vs-src drift.

# Machine-local install/config artifacts
grill-adapter.config.json
shared-wiki-mcp.config.json
manifest-output.json

# OS
.DS_Store
