{
  // Generated by grill-adapter.
  // wiki-researcher OUTPUT contract for schemaVersion 6 Obsidian Carry.
  // This is a transient planning selection. --scaffold consumes it and creates the v6 sidecar.
  // Never include a Note body, destination, task id, taskWikiRefs, or taskFingerprint here.
  "status": "ok",
  "phase": "plan",
  // Copy the exact stable batch identity returned by obsidian_wiki_read_notes.
  "snapshotHash": "sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
  "wikiBindings": [
    {
      "sourceId": "example-project",
      "role": "project",
      "bindingDigest": "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef"
    }
  ],
  "wikiNotes": [
    {
      "sourceId": "example-project",
      "role": "project",
      "path": "Projects/example/constraints/runtime.md",
      "wikiId": "example/constraints/runtime",
      "type": "constraint",
      "constraintStrength": "hard",
      "summary": "Preserve the transaction boundary for runtime writes.",
      "contentHash": "sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
      "bindingDigest": "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
      // Optional normalized UTC knowledge-freshness metadata copied from MCP output.
      // review-due Notes remain selectable with a maintenance warning; expired Notes
      // are excluded by MCP and rejected by Carry.
      "verifiedAt": "2026-07-01T00:00:00Z",
      "reviewAfter": "2027-01-01T00:00:00Z",
      "expiresAt": "2028-01-01T00:00:00Z",
      // Optional, only for a project-owned ADR execution projection. Carry validates the
      // normalized project-relative authority path and its current content identity.
      "adrSourceId": "project-adr:abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789",
      "adrSourcePath": "docs/adr/0001-runtime.md",
      "adrSourceContentHash": "sha256:abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789"
    }
  ],
  // Select Skill Cards independently from normal Notes. requiredFor comes from the Card's
  // `skill_roles` property and controls implementation versus review routing. The
  // name/version/hash fields are returned only after MCP availability validation.
  "requiredSkills": [
    {
      "sourceId": "example-project",
      "role": "project",
      "path": "Projects/example/skills/review-runtime.md",
      "wikiId": "example/skills/review-runtime",
      "type": "guide",
      "summary": "Review the runtime boundary with the project Skill Card.",
      "contentHash": "sha256:abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789",
      "bindingDigest": "0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
      "skillName": "review-runtime",
      "skillVersion": "1.0.0",
      "skillContractHash": "sha256:abcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcdefabcd",
      "skillTriggers": ["runtime review", "transaction boundary review"],
      "discoveryState": "discoverable",
      "requiredFor": ["reviewer"]
    }
  ],
  // Optional transient audit only. When present, there is one reason for every selected
  // Note/Card; each is a one-line paraphrase and must not quote or copy a Note body.
  // Carry validates it and then deliberately omits it from the schema-v6 sidecar.
  "selectionRationales": [
    {
      "wikiId": "example/constraints/runtime",
      "reason": "The requested runtime write change crosses this transaction boundary."
    }
  ],
  "caveats": [],
  // Copy MCP freshness warnings unchanged. Carry also derives any missing review-due
  // warning from the selected Note timestamps.
  "maintenanceWarnings": []
}
