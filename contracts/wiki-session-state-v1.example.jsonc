{
  // Generated by grill-adapter.
  // A local, non-authoritative continuation hint at
  // .grill-adapter/context/<feature-slug>/wiki-session-state.json.
  // It is never an execution input. wiki-readiness must revalidate the roster, sidecar, and
  // frozen role snapshots before any Wiki constraint is used.
  "schemaVersion": 1,
  "kind": "grill-adapter.wiki-session-state",
  "generatedBy": "grill-adapter",
  "featureSlug": "example-feature",

  // The most recent task explicitly selected by readiness/freeze. It is an identifier only:
  // no ticket text, Wiki content, candidate claims, or user data is copied here.
  "lastSelectedTask": "01",

  // Byte digests of the local artifacts at the time this hint was refreshed. snapshotDigest is a
  // canonical digest of the selected task's role-specific snapshot files. Any value may be null
  // before its artifact exists.
  "rosterDigest": "sha256:0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef",
  "contextDigest": "sha256:abcdef0123456789abcdef0123456789abcdef0123456789abcdef0123456789",
  "snapshotDigest": "sha256:1111111111111111111111111111111111111111111111111111111111111111",

  // The selected task's recorded readiness result, or "unrecorded". This is a displayed hint,
  // not a substitute for validation.
  "readinessStatus": "ready",

  // The current number of candidate identities in the local feature journal.
  "candidateCount": 2,

  // The command the next session should use to re-enter the authoritative readiness seam.
  // The generated form uses the current host's native skill prefix (`/` for Claude, `$` for Codex).
  "nextCommand": "$grill-adapter:wiki-readiness 01"
}
